# Le dépôt est déployé sur Linux : tout doit rester en fin de ligne LF.
# Sans cela, les scripts .sh échouent sur le VPS avec « \r: command not found ».
* text=auto eol=lf

*.sh      text eol=lf
*.service text eol=lf
*.conf    text eol=lf
